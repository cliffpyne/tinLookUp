# TRA Lookup-only account (different from the transfer bot's account).
TRA_TIN=170271394
TRA_PASSWORD=463365

# Separate Chrome profile so the two bots can run on the same PC at once.
PROFILE_DIR=./chrome-profile-lookup
HEADLESS=false

# Operator window port (different from the transfer bot which uses 4100).
PORT=4101
